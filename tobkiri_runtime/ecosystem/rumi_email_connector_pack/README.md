# Rumi Email Connector Pack

Owns signed inbound email-webhook normalization and receipt-gated SMTP delivery.
Only attachment metadata is normalized. SMTP requires TLS port 465 or 587 and
public address resolution. Credentials are pack/scope bound. No chat or Company
implementation is imported.

