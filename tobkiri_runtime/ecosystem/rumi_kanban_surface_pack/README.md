# Rumi Kanban Surface Pack

Provides an isolated, read-only `/kanban` route through
`rumi.resource.kanban.v1`. It owns no Kanban state, has no action contract,
and cannot approve or redeem authority receipts. Removing the pack removes
only this UI contribution.

