# Rumi Git Read Pack

Provides status, diff, log, show, branch listing, remote listing, and repository
root inspection. Git arguments are a finite argv allowlist, refs and paths are
validated, output is bounded, and the repository root must be inside the exact
workspace. No write, publish, network, shell, tool, chat, or UI code exists.

