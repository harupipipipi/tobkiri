# Rumi Git Write Pack

Provides receipt-gated stage, commit, branch create/switch, and restore. It has
no push, remote mutation, publication, network, shell, tool, chat, or UI code.
Repository and paths are workspace jailed; Git metadata, environment files,
credential files, and key material are denied.

