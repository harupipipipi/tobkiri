# Rumi HTTP API Connector Pack

Owns constant-time bearer authentication, idempotency-key normalization, and
receipt-gated HTTPS callback delivery. Callback endpoints must resolve publicly
on port 443 and cannot redirect. No chat or Company implementation is imported.

