# Rumi Connector Settings Surface Pack

Owns the removable Connections route and settings contribution. The isolated
surface reads only the connector gateway's redacted, profile-scoped resource.
It renders neither credential handles, credential metadata, nor secret material.

The surface has no mutation, OAuth exchange, transport, registry, credential,
or vendor ownership. Approved changes are supplied by separate action packs.

