# Changelog

- Fixed conversation history actions so the menu escapes scrolling containers,
  isolates row selection/drag/rename gestures, supports keyboard and touch use,
  and can copy the conversation ID with visible success or failure feedback.
- Added defaultspack v2 compatibility layers.
- Added backend loaders, state manager, and setup pack selection helpers.
- Added thin UI module manifests for frontend discovery.
