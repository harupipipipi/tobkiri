# Changelog

- Separated read-only desktop frame capture from explicit, approval-gated,
  revision-bound visual QA evidence with opaque references and bounded private
  retention.
- Added defaultspack v2 compatibility layers.
- Added backend loaders, state manager, and setup pack selection helpers.
- Added thin UI module manifests for frontend discovery.
