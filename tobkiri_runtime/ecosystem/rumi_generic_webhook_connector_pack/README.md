# Rumi Generic Webhook Connector Pack

Provides the `generic_webhook` connector adapter only. Inbound events require
an HMAC-SHA256 signature. Outbound delivery requires a vendor-pack receipt,
pack-scoped credential resolution, credential-free HTTPS on port 443, public
address resolution, and no redirects. It imports neither chat nor Company.

