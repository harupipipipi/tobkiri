# Rumi Mobile Pairing Connector Pack

Owns signed pairing envelopes and a bounded consumed-nonce ledger. Outbound
envelopes are signed and passed to one selected optional mobile relay contract;
the connector has no direct network implementation. No chat or Company
implementation is imported.

