# Rumi Knowledge Store Pack

Owns source knowledge records in one profile-bound atomic state file. Search
indexes and embeddings are non-authoritative projections; the default local
text search requires no network or cloud key. Migration is marker bound.

