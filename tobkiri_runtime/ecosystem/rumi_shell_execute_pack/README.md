# Rumi Shell Execute Pack

Executes bounded commands only under a selected workspace-relative cwd. Every
execution redeems an exact authority receipt; client `approved` flags are not
accepted. Policy classification is external, environment keys are allowlisted,
output is bounded, and timeout terminates the process group.

