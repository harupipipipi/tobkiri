# Rumi P2P Connector Pack

Owns timestamped HMAC peer envelopes and receipt-gated signed HTTPS delivery.
Peer endpoints must resolve publicly on port 443 and cannot redirect. Peer
secrets are pack/scope bound. No chat or Company implementation is imported.

