# Rumi Connector Turn Adapter Pack

Projects normalized connector events into deterministic conversation/message
records and dispatches the global `agent.turn` job action. It imports neither
vendor connectors nor agent implementations and receives no credential
reference. Removing it removes only this projection.

