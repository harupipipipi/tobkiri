# Rumi LINE Connector Pack

Owns LINE HMAC signature verification, one-event normalization, and
receipt-gated reply/push delivery. Reply tokens are emitted only to the current
route projection and are removed by the inbound ledger. Credentials are
pack/scope bound. No chat or Company implementation is imported.

