# Rumi Shell Policy Pack

Tokenizes and classifies commands without executing them. Network, install,
destructive, credential, write-option, and shell-syntax risks are explicit. The
pack imports no filesystem, subprocess, tool, agent, chat, or frontend runtime.

