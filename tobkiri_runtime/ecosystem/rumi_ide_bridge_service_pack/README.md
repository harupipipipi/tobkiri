# Rumi IDE Bridge Service Pack

Owns bounded process-lifetime IDE session envelopes and events. Observe and
control are separate contracts; every create/input/event/close control redeems
an exact authority receipt. It is not an LLM provider and owns no chat, file,
patch, shell, Git, tool, or frontend authority.

