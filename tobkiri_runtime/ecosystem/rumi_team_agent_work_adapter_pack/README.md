# Rumi Company Agent Adapter Pack

Projects Company tasks into deterministic conversation/message records and the
global `agent.turn` job action. It imports neither Company nor agent runtime
implementations and owns no state. Removing it removes only agent work
projection.

