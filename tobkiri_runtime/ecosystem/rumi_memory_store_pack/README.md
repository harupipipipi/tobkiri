# Rumi Memory Store Pack

Owns memory records in one profile-bound atomic state file. Local text search is
a derived read and no SQLite, Markdown, vector, or in-memory secondary owner is
written. Migration and rollback are source-hash and marker bound.

