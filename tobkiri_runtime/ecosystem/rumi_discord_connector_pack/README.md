# Rumi Discord Connector Pack

Owns Discord Ed25519 request verification, interaction normalization, and
receipt-gated channel message delivery. Interaction tokens are not persisted by
the inbound broker. Credentials are pack/scope bound. No chat or Company
implementation is imported.

