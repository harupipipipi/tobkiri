# Rumi Connector Registry Service Pack

Owns connector instance IDs, adapter IDs, enabled state, public settings, and
opaque credential references. Secret-like config keys are rejected. It does not
resolve credentials, verify webhooks, deliver messages, or import chat/Company.

