# Rumi Git Publish Pack

Publishes one validated branch to one configured HTTPS or SSH remote. Every
push/dry-run redeems an exact authority receipt and requires a preflighted
remote URL hash, rechecked after authorization. Local/file/ext transports and
bare force are denied; only optional force-with-lease is exposed.

