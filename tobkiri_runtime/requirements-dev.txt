# Development dependencies for Rumi
# Install: pip install -r requirements-dev.txt

# Keep the clone/dev bootstrap aligned with the checksum-pinned installer bundle.
uv==0.11.14
pytest>=7.0
pytest-asyncio>=0.23.0
# Keep the non-regression baseline stable; Ruff 0.16 enables additional
# diagnostics that require a separately reviewed baseline migration.
ruff==0.15.11
mypy>=1.11.0
