# Development dependencies for Rumi
# Install: pip install -r requirements-dev.txt

# Keep the clone/dev bootstrap aligned with the checksum-pinned installer bundle.
uv==0.11.14
pytest>=7.0
pytest-asyncio>=0.23.0
ruff>=0.6.0
mypy>=1.11.0
